import 'package:flutter/material.dart';

class DetalleActividad1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Detalle Actividad 1')),
      body: Center(child: Text('Detalles de la Actividad 1')),
    );
  }
}
